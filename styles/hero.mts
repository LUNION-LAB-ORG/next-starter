import { heroui } from "@heroui/react";
export default heroui({
    themes: {
        light: {
            colors: {

            }
        },
        dark: {
            colors: {
                background: "#0C0C0C"
            }
        }
    },
    addCommonColors: true,
    defaultTheme: "light",
});